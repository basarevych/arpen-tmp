/**
 * Default module configuration
 */
module.exports = {
    // Load module classes and services
    autoload: [
        'module.js',
        'routes',
    ],

    // View directories
    views: [
        'views',
    ],

    // Static directories
    static: [
        'public',
    ]
};
